# Simulation models

将来のモデル資産を置く領域です。現時点の暫定パラメータは単一 HTML 内にあります。

追加時は `wok/`、`rice/`、`egg/`、`gesture/` のように対象別のサブディレクトリを作り、単位、座標系、出典、ライセンス、バージョンを README に記録してください。公開アプリは最終的に単一 HTML とするため、ビルド時に必要なデータだけを埋め込みます。大きな学習済みモデルを安易に Git へ追加せず、容量とブラウザ実行性能を先に評価してください。
