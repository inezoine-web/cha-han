# Development log

## 2026-08-16 — repository baseline and playable prototype

- `idea.md` を製品構想の一次資料として維持した。
- 仮名だった `test.html` を編集元 `progs/cha-han.html` へ移し、公開成果物を `release/index.html` に統一した。
- 初期実装をレビューし、配列を走査中に `splice` する粒子欠落、画面外へはみ出す鍋、起動時に生成される AudioContext、センサーリスナーの重複を解消した。
- センサーがない PC でも確認できる左右ボタンとキーボード操作、停止、状態表示、安全上の注意を追加した。
- センサー値は絶対位置へ積分せず、短い慣性入力として減衰させる。これは `idea.md` の「一振り単位」という方針に合わせた暫定モデルである。
- GitHub Pages 用 workflow、再現可能なビルド、静的チェック、運用文書を追加した。

## Next steps

1. 複数端末で DeviceMotion の軸・符号・サンプリング間隔を記録する。
2. 一振りを開始・加速・減速・回収のフェーズへ分ける。
3. 実鍋の直径と深さを `models/wok/` のデータへ落とし込み、画面座標と物理単位を分離する。
4. 粒子間衝突より先に、再現可能な入力波形と評価指標を用意する。

## 2026-08-16 — 上面視の機能試作へ縮小

- ゲーム的な残量ゲージや左右ボタンを外し、画面を上にして右手で持つ前提の円形・上面視ワイヤーフレームへ変更した。
- 一般的な丸底中華鍋を回転放物面として近似し、軽量な48粒の米に重力、鍋面の中心向き成分、摩擦、飛翔を適用する。厳密な粒子間衝突は試作段階では行わない。
- リセット操作を、米の再配置、モーションセンサー権限取得、最初のセンサー標本による水平基準調整の一連の入口に統合した。
- センサー強度を0.2〜2.5倍で調整できるようにし、センサーのない開発環境向けに円形領域のドラッグと4方向キーをフォールバックとして残した。
- `./scripts/build.sh` と `./scripts/check.sh` を通し、Playwright 1.62.1 と Google Chrome でモバイル表示、リセット、スライダー、ドラッグ操作を確認した。

## 2026-08-16 — 実鍋の体感から米の運動仮説を整理

- 調理時の観察として、米の集団は静置しても油のように中央へ流れず、鍋肌・粒同士・油や水分による降伏抵抗を持つという仮説を記録した。
- 鍋振りの主要因を単純な上下動ではなく、前進加速、前進減速、曲面上昇、離床、引き戻して回収する一連の位相として整理した。
- 次の試作候補として、有効重力 `g - A_w`、静止摩擦、固着・滑り・飛翔の状態遷移、移動する鍋との再衝突を `docs/rice-motion-model.md` にまとめた。
- これらは体感に基づく未検証のモデルであり、再現入力、センサー記録、実鍋の動画と照合して定数と妥当性を検証する。

## 2026-08-16 — 有効重力と状態遷移の成立性試作

- 上面視の常時復元モデルを断面視へ置き換え、鍋の並進加速度を差し引いた接線方向の力で米を動かす試作にした。
- 代表36粒を `STUCK` / `SLIDING` / `AIRBORNE` / 消失に分けた。静止摩擦と凝集抵抗を越えた粒だけが滑り、離床後は重力で飛翔し、移動中の放物面へ再接触する。
- 同じ条件を比較できる「自動で一振り」を追加し、押し出し、急減速と軽い上振り、引き戻しの入力波形を約1.2秒で再生する。ドラッグ、左右キー、端末モーション入力も維持した。
- 成立性の確認段階で調整が予想される入力強度、静止・動摩擦、凝集抵抗、鍋の深さ、離床感度、着地反発、粒ごとのばらつきを画面上のスライダーへ公開した。値は計測済み定数ではなく比較実験用である。
- `./scripts/build.sh`、`./scripts/check.sh`、`git diff --check` を通した。Playwright 1.62.1 と Google Chrome 151、Noto CJK フォントで390×844のモバイル表示を確認し、スクリーンショットは `/tmp` のみに保存した。

## 2026-08-16 — 断面鍋の上下を修正

- 断面図の放物線が中央の盛り上がった蓋状になっていたため、左右の縁を高く、中央の底を低く表示する丸底鍋へ反転した。
- 描画だけでなく、粒の接触面と勾配も同じ座標系へ直し、米が見た目どおり鍋の内側に載るようにした。鍋本体は内面に沿う一定幅の帯として描き、取っ手を右の縁へ接続した。
- `./scripts/build.sh`、`./scripts/check.sh`、`git diff --check` を通し、Playwright 1.62.1 と Google Chrome でモバイル表示と自動一振りを確認した。

## 2026-08-17 — 米が鍋肌を貫いて見える問題を修正

- 飛翔粒が鍋の接触面より下へ入った場合は、下降中だけでなく上昇中でも必ず接触面へ戻すようにした。大きなフレーム間隔や鍋の移動が重なっても、粒が鍋を裏側から通り抜けない。
- 鍋の輪郭線を米より手前に描画し、鍋上の米には飛翔粒用の影を付けないようにした。接触中の米の下端を鍋縁が隠すため、米が金属帯を貫いているように見えない。
- `./scripts/build.sh`、`./scripts/check.sh`、`git diff --check` を通し、Playwright 1.62.1 と Google Chrome で 390×844 のモバイル表示を確認した。

## 2026-08-20 — 現行モデルのランダム広域探索

- 現行の自動一振り波形を固定したまま、入力強度、摩擦、凝集抵抗、鍋の深さ、離床感度、着地反発、粒ごとのばらつきの8項目から2,500設定をランダムサンプリングする広域探索を追加した。
- 各設定を描画なしで3秒間計算し、離床率、回収率、残存率、損失率から暫定スコアを算出する。上位500件をブラウザ内へ保存し、一覧から選んだ値をスライダーへ反映して通常速度で再生できるようにした。
- 探索中は25試行ごとに画面へ制御を返し、モバイルブラウザを長時間占有しないようにした。探索結果は現行式と固定波形に対する感度を見るもので、実際の物理定数の推定値ではない。
- `./scripts/build.sh`、`./scripts/check.sh`、`git diff --check` と JavaScript 構文検査を通した。Playwright 1.62.1、Google Chrome 151、Noto CJK フォント、390×844の表示で、2,500設定の探索完了、上位500件の保存、2位の選択再生、実行時エラーがないことを確認した。

## 2026-08-20 — 参照動画の鍋運動を分解

- 参照された鍋振り動画を、正解軌道の数値資料ではなく、必要な自由度・拘束・位相を洗い出す定性的な標本として整理した。
- 鍋運動を前後並進、上下並進、ピッチ、移動する瞬間中心、補助的なロール／ヨーへ分け、「準備→送り出し→制動・斜面上昇→すくい・離床→飛翔・反転→回収・吸収→次周期」の状態機械にした。
- シミュレーションではピッチと回転面速度、食材では離床・着地の時間幅、評価では制動・ピッチ・離床・回収の位相差を次に実装・計測すべき要素とした。
- 単眼動画から物理定数を断定しない限界を明記し、鍋と食材の特徴点追跡、鍋径・周期による正規化、IMUとの同期を行う計測手順を記録した。

## 2026-08-20 — 論文と参照動画から「米が舞う」共通因子を統合

- Ko & Hu (2020) の業務用レンジ上のシーソー式鍋振りと、片手鍋の参照動画を、鍋軌道そのものではなく米側の離床条件から比較した。
- 共通因子を「曲面側への助走」「接線方向速度の生成」「接線速度が残る時刻で法線拘束を解く」「飛翔中に鍋を回収位置へ移動」の4段階として整理した。
- 良い一振りを `助走 → リリース → 飛翔 → 回収` と一般化し、異なる鍋振りスタイルでも局所表面速度 `V_w + omega × r` と法線拘束を用いて同じ米モデルへ接続できる仮説を `docs/common-airborne-factors.md` に記録した。
- 探索では絶対軌道より、接線速度ピーク、減速ピーク、ピッチ角速度ピーク、離床、回収開始の位相差を主要特徴量にする方針を候補とした。論文の測定事実と、この統合一般化の仮説を明確に分けて記述した。
